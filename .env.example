# credentials
SAUCEDEMO_USER=...
SAUCEDEMO_PASSWORD=...

# Local environment / run options (do NOT commit .env to VCS)
# Base URL for tests
BASE_URL=https://www.saucedemo.com

# Headless mode: "true" or "false"
# Use false for headed (visible) mode
HEADLESS=true

# Slow motion in ms (0 = no slow-mo). Useful for debugging.
SLOW_MO=0

# Trace collection: 'off' | 'on' | 'on-first-retry' | 'on-all-retries' | 'retain-on-failure' | 'retain-on-first-failure'
TRACE=retain-on-failure

# Video recording: 'off' | 'on' | 'retain-on-failure' | 'on-first-retry'
VIDEO=retain-on-failure

# Screenshot policy: 'off' | 'on' | 'only-on-failure' | 'on-first-failure'
SCREENSHOT=only-on-failure

# Number of parallel workers (leave empty to allow Playwright to auto-detect)
WORKERS=

# Number of retries (if empty, defaults to 2 in CI, 0 locally)
RETRIES=

# CI detection (some CI systems set CI=true automatically)
# Optionally override locally:
CI=false

